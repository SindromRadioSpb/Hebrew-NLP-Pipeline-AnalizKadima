# requirements.txt — pinned production deps
# Source of truth: pyproject.toml (version ranges)
# This file: exact pins for reproducible Docker builds
#
# Generate:  pip install -e . && pip freeze > requirements.lock
# Update:    1) edit pyproject.toml ranges
#            2) pip install <pkg>==<ver>
#            3) update this file with exact pins

PyYAML==6.0.1
pydantic==2.10.6
spacy==3.8.4
httpx==0.28.1
fastapi==0.115.8
uvicorn[standard]==0.34.0
